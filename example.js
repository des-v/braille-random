// Generate a random dot6 braille characters from a given amount of characters.
// Usage example:
const brailleRandom = require('braille-random')

brailleRandom(3) // Call function with a

console.log(brailleRandom(3)) // random print: ⠭⠒⠇
